import { Button } from "@mui/material";
// import React, { useEffect } from "react";
import "./Home.scss";

const Home = () => {
  return (
    <div className="home">
      <h1>Nahlás svoju neprítomnosť na vyučovaní </h1>
      <Button variant="contained">TU!</Button>
    </div>
  );
};

export default Home;
